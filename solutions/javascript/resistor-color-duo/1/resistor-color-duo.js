export const decodedValue = duo => Number.parseInt(`${COLORS.indexOf(duo[0])}${COLORS.indexOf(duo[1])}`);

export const COLORS = [
      'black',
      'brown',
      'red',
      'orange',
      'yellow',
      'green',
      'blue',
      'violet',
      'grey',
      'white',
    ];

